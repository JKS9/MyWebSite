<%- include('../layouts/head'); -%>
<section class="hero is-medium is-primary is-bold">
    <div class="hero-body">
      <div class="container">
        <h1 class="title">
          Primary bold title
        </h1>
        <h2 class="subtitle">
          <% if(locals.meta) { %>
            ok meta
          <% } else { %>
            pas de meta
          <% } %>
        </h2>
      </div>
    </div>
</section>
<%- include('../layouts/footer'); -%>